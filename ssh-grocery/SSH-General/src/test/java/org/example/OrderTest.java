package org.example;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import java.sql.SQLException;

import static org.junit.jupiter.api.Assertions.assertEquals;

class OrderTest {
    /*

    @Test
    void testGetTargetSubOrderID() throws SQLException {
        // The test is from actual data in the database
        // This test will fail in GitLab because the database is not accessible from the CI/CD pipeline
        // You can run this test locally to see the expected result

        Data.User currentUser = new Data.User("af9fb1e6-7df1-4db7-9436-53c9db6861df", "Louis", "admin_4", 100002);

        int subOrderID = Order.getTargetOrderID(currentUser);
        assertEquals(500004, subOrderID);
    }

     */

}